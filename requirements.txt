opencv-python==4.9.0.80
numpy==1.26.2
scipy>=1.7.0
Pillow>=9.0.0
pyfiglet==1.0.2
# Tkinter (used by the GUI) ships with standard Python distributions.
# On some Linux systems you may need to install it separately, e.g.
# `sudo apt-get install python3-tk`.